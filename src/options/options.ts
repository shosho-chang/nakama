// Options entry point. S1 skeleton.
// S3 will add: vault folder picker (FSA) + handle status / re-pick button.

const status = document.getElementById("status");
if (status) {
  status.textContent = "Options page skeleton. Vault picker arrives in S3.";
}
