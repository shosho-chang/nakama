# Auto-generated from pyproject.toml — 保留此檔案供 VPS 上 pip install -r 使用
# 修改依賴請改 pyproject.toml，再同步到這裡

# Core
anthropic>=0.42.0
pyyaml>=6.0
python-dotenv>=1.0

# Web & API
httpx>=0.27.0
feedparser>=6.0        # RSS (PubMed, etc.)
firecrawl-py>=4.22.0   # Firecrawl: web scraping/crawling for Robin & Zoro

# Database
# sqlite3 is built-in

# Zoro: Keyword Research
google-api-python-client>=2.100.0  # YouTube Data API v3
pytrends>=4.9.0                    # Google Trends (unofficial)

# Transcription (post-processing; openlrc 需另裝，見 pyproject.toml)
opencc-python-reimplemented>=0.1.7
# GPU 環境另裝：pip install openlrc --no-deps && pip install faster-whisper ctranslate2

# Utilities
python-dateutil>=2.9
psutil>=5.9.0              # Franky: system health checks (disk/memory)

# Web UI (Robin)
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
jinja2>=3.1.0
python-multipart>=0.0.9

# Slack Gateway
slack-bolt>=1.18.0
slack-sdk>=3.27.0
