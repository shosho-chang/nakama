# ADR-039 Panel Integration Matrix (3-way)

**Date:** 2026-05-31
**Panel:** Claude Opus 4.8 (drafter) · Codex/GPT-5 (audit, read-only grounding in worktree) · Gemini 2.5 Pro (audit, read ADR + Codex)
**Audits:** [`2026-05-31-codex-adr039-audit.md`](2026-05-31-codex-adr039-audit.md) · [`2026-05-31-gemini-adr039-audit.md`](2026-05-31-gemini-adr039-audit.md)
**Both verdicts:** Approve **with modifications** — Slice 0 safe now; Slices 1–2 gated on the changes below.

Legend — pattern: `3-way` (both auditors flag) · `2of3` · `single`. Resolution: `ADOPT` · `MODIFY` · `ESCALATE` (user call) · `ACK` (document as accepted trade-off).

| # | Topic | Claude v1 | Codex | Gemini | Pattern | Resolution |
|---|---|---|---|---|---|---|
| 1 | Derived aggregates in weekly frontmatter | D1/D5 kept `planned/actual_pomodoros` cache | §2/§6 — contradicts D3 "computed on read"; drop or label stale w/ `computed_at` | Prior1/§6#5 — a Journal is a primary source, **not a cache**; compute on read | 3-way | **ADOPT** — remove `planned/actual_pomodoros` from weekly frontmatter; compute on read every time |
| 2 | D5 carve-out breadth / principle | "provenance" (human-input ≠ agent write) | §2/§4 — too broad vs ADR-028's Journals exception that "does NOT generalize" (`:179-182`); blast-radius; allowlisted writer; explicit non-generalize clause | §4/§6#1 — "seductive but corrosive" slippery slope; reframe to **structure-vs-prose** | 3-way | **MODIFY + ESCALATE** — synthesize: human-typed-vs-machine-generated (authz) + structure-vs-prose (machine may touch only enumerated structured frontmatter, never prose) + Codex's non-generalize/blast-radius/allowlisted-writer. Exact wording → user |
| 3 | D4 `scheduled` auto-sync | auto-rewrite `scheduled` = earliest incomplete `plan[]` date | §5 Alt1 — don't auto-rewrite; treat `scheduled` as convenience projection; surface divergence | §4/§6#2 — modeled "task vanishes June 2 / appears June 9" in Obsidian = surprising; explicit sync button | 3-way | **ADOPT** — `plan[]` SoT; `scheduled` user-controlled; Bridge offers explicit "sync to next planned date" + surfaces "scheduled differs from plan" |
| 4 | D3 "never double-count" claim | union claimed to never duplicate | §3 — false as architectural invariant; need deterministic dedup/overlap (normalized records `source/source_id/task_key/start/end`; overlap-within-tolerance → warn, not sum) | §3#1 — bigger risk is "**no count at all**": abandoned Bridge session has no `completed` flag → undercount | 2of3 + complementary | **ADOPT both** — dedup/overlap detection + defined state for in-progress/abandoned Bridge sessions |
| 5 | D3 time semantics | under-specified | §3 — count `type==work && completed`; use `activePeriods` for duration (pauses inflate); `endTime`→Asia/Taipei as week-assignment key (not filename); define sessions-vs-minutes | §3#3/§6#4 — explicit Sat→Sun boundary rule + test (23:50 Sat → 00:15 Sun) | 3-way | **ADOPT** — full time-semantics spec + Sat→Sun boundary test |
| 6 | Concurrency / lost update | "single-user ⇒ concurrency ~0" | §4 — single-user ≠ no lost-update (Syncthing + Obsidian); need `If-Match` mtime/hash → 409; `update_frontmatter`/`update_body_section` lack the mtime guard `append_timeentry` has | (implicit) | Codex-strong | **ADOPT** — `weekly_writer` carries `If-Match` mtime/hash from the rendered page → reject stale with 409; atomic rename ≠ lost-update protection |
| 7 | D2 two-week-identity | "fully decouples" from ISO infra | §3 — year-boundary math: derive `year`/`week_number` from week-year (not `start_date.year`); tests for 2025-12-28..2026-01-03 + 2026-12-27..2027-01-02 | §1/§6#3 — it's a conceptual **schism**, not a broken backlink; heal via `shosho_week:` backlink written into the 7 daily notes | 2of3 (diff angles) | **MODIFY + ESCALATE** — soften "fully decouples"; ADOPT week-year derivation + boundary tests; the `shosho_week` heal needs writing Daily (🔒) → user call: accept-debt vs heal |
| 8 | Governance drift | edited VAULT-LAYOUT/CONTEXT to 🟡 + cited `bridge_weekly.py` (doesn't exist) pre-acceptance | §1/§4 — revert or mark clearly proposed; don't let canonical docs describe non-existent code as producer reality | §5#3 — serious process concern | 3-way | **ADOPT** — mark the doc edits "(Proposed — ADR-039 pending acceptance)"; keep on ADR branch (the PR is the acceptance vehicle) but don't assert accepted reality |
| 9 | D6 completion field | "writes `status`/`✅`" | §1/§2 — code uses `status ∈ {to-do,doing,done,paused}` (`project_writer.py:391`), not `✅`; `update_task_status` doesn't sync the boolean; pick one canonical + atomic | — | Codex-factual | **ADOPT** — canonical = `status` enum; fix the `✅` factual error; if boolean kept, update atomically |
| 10 | Weekly reader conflict-file detection | listed as backlog | §1 — `digest_indexer` already detects `*.sync-conflict-*` (`:35-40,:147-172`); don't ship a new reader without equivalent | — | Codex | **ADOPT** — move conflict-file detection into Slice 0 `weekly_indexer` (not backlog) |
| 11 | 25-min pomodoro source | "TaskNotes config is authority" but Bridge hard-codes `POMODORO_MINUTES=25` | §3 — first config change silently splits Obsidian/Bridge math; read a shared constant | — | Codex | **MODIFY** — v1 keeps 25 but document the coupling + TODO single shared source |
| 12 | Two-timer UX | union read, silent | — | Prior3/§3#3 — cognitive load: two timer UIs → two invisible stores, no in-app explanation | single (merit) | **ADOPT** — add UX framing: Obsidian timer = deep-work tied to a day; Bridge dock = ad-hoc task time; show a one-line hint |
| 13 | 隨手筆記 usability | week-file section | — | §3#2 — fleeting capture wants an in-the-moment tool; nav to `/bridge/weekly` = friction; will be ignored vs Obsidian daily/inbox | single (merit) | **MODIFY** — clarify 隨手筆記 = **weekly-scoped reflection**, NOT a fleeting-capture inbox (quick capture stays in Obsidian/Inbox) |
| 14 | Alternative: tag-based plan + dataview | not documented | — | §5#2 — simpler `#plan/2026-06-02` + dataview (read-only Bridge) "not adequately considered" | single (merit) | **ADOPT into Alternatives** — document + reject: tags can't carry per-date pomodoro **count**; dataview is desktop-only (the exact VPS blocker that motivates Tier B) |
| 15 | Vault-as-backing-store / privileging Web over Obsidian | accepted (ADR-031 continuation) | — | §5#1 — `plan[]` invisible to TaskNotes degrades vault-as-standalone; "parasitic integration" | single (philosophical) | **ACK** — document in Consequences as a conscious trade-off; mitigated by #3 (no magical `scheduled` rewrites). Not reversed |

## Rollup

- **Clear ADOPT (fold into v2 directly):** #1, #3, #4, #5, #6, #8, #9, #10, #11, #12, #13, #14, #15.
- **ESCALATE to 修修 (user call before finalizing v2):**
  - **#2 — D5 principle wording** (provenance vs structure-vs-prose synthesis). Recommended: the synthesis (human-typed authz + machine-restricted-to-structured-frontmatter + non-generalize clause).
  - **#7 — D2 week schism** (accept as documented debt, Daily stays 🔒 read-only — recommended for v1; vs heal via `shosho_week` backlink into Daily = a second 🔒 carve-out).
- **Net effect:** v1 was structurally sound (both approve) but had (a) one internal contradiction (#1), (b) one over-broad principle (#2), (c) two over-claims ("never double-count" #4, "fully decouples" #7), (d) one factual error (#9 `✅`), and (e) a governance process slip (#8). None are fatal; all are fixable in v2. The panel earned its keep on #2 (structure-vs-prose), #4 (dedup is not free), #6 (lost-update), and #7 (schism framing) — none of which single-Claude surfaced.
