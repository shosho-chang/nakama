// (deprecated) Hyperframes does not need a config file — `index.html` plays as-is.
// File kept as a no-op placeholder to avoid blocking PR #320 on a separate
// deletion permission. Safe to delete in any subsequent PR; tsconfig.json no
// longer includes it and package.json no longer references Remotion.
export {};
