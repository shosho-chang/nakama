// Remotion configuration for nakama script-driven video production.
// Scene components, codec settings, and rendering configuration
// are added in Slice 2 when Remotion deps are installed via npm install.

export {};
