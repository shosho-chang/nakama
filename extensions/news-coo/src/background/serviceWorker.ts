// Service worker entry point. S1 skeleton.
// Future: S2 routes EXTRACT messages, S5 registers context menu, S5 handles command shortcuts.

chrome.runtime.onInstalled.addListener(() => {
  console.log("[News Coo] installed");
});
