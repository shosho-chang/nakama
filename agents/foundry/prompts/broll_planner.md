# broll_planner — canonical prompt template

> Placeholder ship 在 PR-1。完整 prompt template 在 PR-3 寫進來（含 brand
> tokens load、STYLE.md 載入、guardrails enforcement、anchor exact-copy
> contract、output YAML schema 範例）。

PR-3 (#714) 補。
